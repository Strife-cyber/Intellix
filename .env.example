# Shared HMAC secret — must match CER_SHARED_SECRET in Intellix (.env)
CER_SHARED_SECRET=change-me-shared-secret

# Optional: fallback AI provider when no provider is passed in the job payload (dev only)
# AI_PROVIDER=ollama
# OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_MODEL=llama3.1
